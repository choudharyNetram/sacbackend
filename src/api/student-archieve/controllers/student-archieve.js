'use strict';

/**
 * student-archieve controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::student-archieve.student-archieve');
